-- notmuch.attach.incoming.attachment -- Attachment object builder

local A = {}

function A.from_part(part, message_id, path)
  error('notmuch.attach.incoming.attachment.from_part is not implemented yet')
end

return A
